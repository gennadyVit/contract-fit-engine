# Schema

DDL for all raw staging tables in `GOVCONTRACT.RAW`.

Run `init.sql` to create all tables from scratch in a new environment.
dbt manages the DIMENSIONS and MARTS schemas — run `dbt run` after init.
